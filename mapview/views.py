# Les vues de l'app mapview ont été migrées vers core/views.py (HomeViewSet.map_data).
# Map views have been moved to core/views.py (HomeViewSet.map_data).
# Les modèles Marker et MapViewConfig sont conservés dans models.py.
# Marker and MapViewConfig models are kept in models.py.
